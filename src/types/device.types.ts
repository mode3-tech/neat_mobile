export interface DeviceInfo {
  device_id: string;
  public_key: string;
  device_name: string;
  device_model: string;
  os: string;
  os_version: string;
  app_version: string;
  // user_agent: string;
}
